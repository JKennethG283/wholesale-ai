import { PlaceholderPage } from "@/components/placeholder-page";

export default function WorkflowLogPage() {
  return (
    <PlaceholderPage
      title="Workflow Log"
      summary="Stock updates, email drafts, n8n triggers, tickets, and report events will land here."
    />
  );
}
